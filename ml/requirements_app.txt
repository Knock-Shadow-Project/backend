# Pinned subset for the Streamlit UI container (Dockerfile.ml-app).
# Keep in sync with ml/uv.lock; see requirements.txt for the bump procedure.
streamlit==1.57.0
plotly==6.7.0
pandas==3.0.3
numpy==2.4.4
scipy==1.17.1
structlog==25.4.0
pydantic==2.10.4
