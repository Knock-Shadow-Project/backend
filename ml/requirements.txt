# Pinned for reproducible Docker builds. Versions match ml/uv.lock so local
# development with `uv` and container builds resolve to the same wheels.
#
# Bumping: run `uv lock --upgrade-package <name>` and copy the new version
# back here. Do not switch to `>=` ranges — unrelated upstream releases
# (esp. torch/numpy/pandas) have repeatedly broken model loading.

# app + pipeline
psycopg2-binary==2.9.12
pandas==3.0.2
numpy==2.4.4
scipy==1.17.1
streamlit==1.57.0
plotly==6.7.0
requests==2.33.1
websockets==16.0
structlog==25.4.0
pydantic==2.10.4

# training only (tensorflow no soporta Python 3.14, usar PyTorch)
torch==2.12.0
scikit-learn==1.8.0
matplotlib==3.10.9
seaborn==0.13.2
